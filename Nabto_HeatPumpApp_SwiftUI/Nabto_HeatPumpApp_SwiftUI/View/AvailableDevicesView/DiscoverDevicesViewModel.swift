//
//  PairNewDeviceViewModel.swift
//  Nabto_HeatPumpApp_SwiftUI
//
//  Created by Christopher Larsen on 05/12/2021.
//

import Foundation
import SwiftUI
import NabtoEdgeClient


final class DiscoverDevicesViewModel : ObservableObject{
    /*
    
    @ObservedObject var reciever = MdnsRecieve()
    
    var client: Client
    var scanner: MdnsScanner
    
    init(){
        client = Client()
        scanner = client.createMdnsScanner()
        
        do {
            scanner.addMdnsResultReceiver(reciever)
            
            try scanner.start()
            
            print("Scanner is started")
            
            print("List is: \($reciever.discoveredDevices.count)")

            
        }catch NabtoEdgeClientError.INVALID_ARGUMENT{
            print("Invalid level")
        }
        catch NabtoEdgeClientError.INVALID_STATE{
            print("The scan could not be started, e.g. the client is being stopped")
        }
        catch let error{
            print(error)
        }
        

    }
    
    public func startScanner(){
        do {
            scanner.addMdnsResultReceiver(reciever)
            
            try scanner.start()
            
            print("Scanner is started")
            
            print("List is: \($reciever.discoveredDevices.count)")

            
        }catch NabtoEdgeClientError.INVALID_ARGUMENT{
            print("Invalid level")
        }
        catch NabtoEdgeClientError.INVALID_STATE{
            print("The scan could not be started, e.g. the client is being stopped")
        }
        catch let error{
            print(error)
        }
        
    }
    
    public func stopScanner(){
        scanner.stop()
    }

    
    */
    
    
}




